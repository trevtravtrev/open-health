import React from 'react';

// External nav links (Clinic, Q&A, Feedback, Github, Reddit, Discord) were
// removed per request. Kept as a no-op so existing call sites render nothing.
export function NavLinks() {
    return null;
}
